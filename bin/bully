#!/usr/bin/env node

var path = require('path'),
    fs   = require('fs');
    //lib  = ;
require.paths.unshift(path.join(path.dirname(fs.realpathSync(__filename)), '../compiler'));

require('command').run();

